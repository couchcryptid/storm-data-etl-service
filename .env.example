KAFKA_BROKERS=localhost:9092
KAFKA_SOURCE_TOPIC=raw-weather-reports
KAFKA_SINK_TOPIC=transformed-weather-data
KAFKA_GROUP_ID=storm-data-etl
HTTP_ADDR=:8080
LOG_LEVEL=info
LOG_FORMAT=json
SHUTDOWN_TIMEOUT=10s
BATCH_SIZE=50
BATCH_FLUSH_INTERVAL=500ms

# Mapbox geocoding (feature-flagged; token auto-enables geocoding)
# MAPBOX_TOKEN is a secret -- set it in .env (gitignored) or export in your shell.
# MAPBOX_ENABLED=true
# MAPBOX_TIMEOUT=5s
# MAPBOX_CACHE_SIZE=1000
